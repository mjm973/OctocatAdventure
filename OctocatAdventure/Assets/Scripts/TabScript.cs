using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TabScript : MonoBehaviour {

//	GameObject[] playerControls; 
//	public bool tabIsOn = true; 

	// Use this for initialization
	void Start () {
//		playerControls = GameObject.FindGameObjectsWithTag ("PlayerControl");
//		Debug.Log (playerControls);
	}
	
	// Update is called once per frame
	void Update () {

//		tabIsOn = this.GetComponent<Toggle>().isOn;
//
//		if (tabIsOn) {
//			foreach (var i in playerControls) {
//				i.SetActive (true);
//			}
//		}
	}
}
